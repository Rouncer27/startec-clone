---
import "./footerNav.scss";
---

<div class="footer-nav">
  <ul class="footer-nav-items">
    {
      Astro.props.items.map((item) => {
        console.log(item);
        const classes = item.cssClasses.join(" ");
        return (
          <li class={`footer-nav-item ${classes}`}>
            <a href={`${item.uri}`}>{item.label}</a>
          </li>
        );
      })
    }
  </ul>
</div>
